import 'package:flutter_test/flutter_test.dart';

void main() {
  // TODO(test): make tests
  group('PokemonCardWidget integrity |', () {
    testWidgets('Check PokemonCardWidget integrity', (tester) async {
      // on loading should have a progress indicator
      // should have the id, the name, and image of the pokemon
    });
  });

  group('PokemonCardWidget behavior |', () {
    testWidgets('When to click on card should be called the onTap callback', (tester) async {});
  });
}
